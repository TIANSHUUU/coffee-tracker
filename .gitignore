__pycache__/
*.pyc
*.pyo
.env
.DS_Store

# Keep output JSON for "new item" comparison, skip binary/csv
output/*.csv
output/*.xlsx

# Raw diary source notes (photos/markdown) — processed into docs/diary.json, not published
coffee-diary/

# Wrangler's local account cache — machine-specific, not part of the app
worker/*/.wrangler/

# Local Claude Code tooling (skills, agents, lockfile) — not part of the app
.claude/
.agents/
skills-lock.json
